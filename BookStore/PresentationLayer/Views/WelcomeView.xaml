<UserControl x:Class="PresentationLayer.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Border CornerRadius="16" Background="{StaticResource CardBrush}" Padding="48" Margin="48" Effect="{StaticResource CosmoShadow}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Chào mừng đến với BookStore!" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,24" HorizontalAlignment="Center"/>
            <TextBlock Text="Quản lý sách, đơn hàng, người dùng và nhiều hơn nữa với giao diện hiện đại." FontSize="18" Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,32" HorizontalAlignment="Center"/>
            <Button Style="{StaticResource CosmoButton}" Content="Bắt đầu" Command="{Binding StartCommand}" Width="180" Height="48" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl> 